import { statusIndex,statusSteps } from "@/lib/status";
export default function StatusBar({status}:{status:string}){const idx=statusIndex(status);return <div className="flex items-start overflow-x-auto py-4">{statusSteps.map((step,i)=><div className="flex flex-1 items-start" key={step}><div className="min-w-[150px] text-center text-xs font-black text-slate-600"><div className={"mx-auto mb-2 grid h-11 w-11 place-items-center rounded-full text-base font-black text-white "+(i<idx?"bg-green-600":i===idx?"bg-amber-500":"bg-slate-300")}>{i<idx?"✓":i+1}</div>{step}</div>{i<statusSteps.length-1&&<div className={"status-line mt-5 "+(i<idx?"done":"")}/>}</div>)}</div>}
