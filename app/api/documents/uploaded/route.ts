import {NextRequest,NextResponse} from "next/server";import {supabaseAdmin} from "@/lib/supabaseAdmin";
export async function POST(req:NextRequest){const b=await req.json(),s=supabaseAdmin();const{data:doc,error}=await s.from("lead_documents").update({status:"Uploaded",file_path:b.filePath,file_name:b.fileName,uploaded_at:new Date().toISOString()}).eq("id",b.documentId).select().single();if(error)return NextResponse.json({error:error.message},{status:500});await s.from("lead_notes").insert({lead_id:doc.lead_id,note:`Client uploaded: ${doc.document_type}`});return NextResponse.json({ok:true})}
