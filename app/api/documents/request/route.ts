import {NextRequest,NextResponse} from "next/server";import {supabaseAdmin} from "@/lib/supabaseAdmin";
export async function POST(req:NextRequest){const b=await req.json(),s=supabaseAdmin();const{error}=await s.from("lead_documents").insert({lead_id:b.leadId,document_type:b.documentType,note:b.note||"",status:"Requested"});if(error)return NextResponse.json({error:error.message},{status:500});await s.from("leads").update({status:"Documents Requested",updated_at:new Date().toISOString()}).eq("id",b.leadId);await s.from("lead_notes").insert({lead_id:b.leadId,note:`Document requested: ${b.documentType}${b.note?" - "+b.note:""}`});return NextResponse.json({ok:true})}
